@model Dmart.Models.OrderModel


<tbody class="trsOfProducts">

        @foreach (var item in Model.ProductList)
        {
            <tr class="@item.OrderId">
                <td> @Html.DisplayFor(modelPrd => item.ProductId)</td>
                <td> @Html.DisplayFor(modelPrd => item.Name)</td>
                <td> @Html.DisplayFor(modelPrd => item.Quantity)</td>
                <td> @Html.DisplayFor(modelPrd => item.unitPrice)</td>
            </tr>
        }

    </tbody>

        @*@foreach (var prd in Model.ProductList.Where(x => x.OrderId == item.OrderId).OrderBy(x => x.ProductId))
            {

             @Html.Partial("/Views/Home/_IndexProductList.cshtml",prd);

            }*@

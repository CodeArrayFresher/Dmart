
@{
    ViewBag.Title = "Jqeury";
}

<div>
    <ul>
        <li><p>one</p></li>
        <li><p>two</p></li>
        <li><p>three</p></li>
        <li><p>four</p></li>
    </ul>
</div>


<script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
<script>
        //alert(jQuery('a').filter('.external').length + ' external links');
        //alert(
        //    jQuery('a')
        //        .filter(function (index) { return $(this).hasClass('external'); })
        //        .length + ' external links'
        //);
        //alert('total em are' + $('p').find('em').length );
    //alert(jQuery('p').filter('.middle').find('span').end().length); //end is used to undo
    //alert(jQuery('p').filter('.middle').length);

    //jQuery('div').find('p').andSelf.css('border', '1px solid #993300'); andself include prior selection

    //jQuery('li:eq(1)').next().css("color", "red");
    //jQuery('li:eq(1)').prev().css("color", "red");
    //jQuery('li:eq(1)').parent().css("color", "red"); //selects the first <li>
    //jQuery('li:eq(1)').parent().children().css("color", "red");
    //jQuery('li:eq(1)').nextAll().css("color", "red");
    jQuery('li:eq(1)').prevAll().css("color", "red");
</script>
